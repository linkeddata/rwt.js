common = require('common');

exports.options = function(callback) {
    xthis = this;
    common.req('OPTIONS', '/', '', function(s, h, d) {
        xthis.status=s;xthis.headers=h;xthis.data=d;
        callback();
    });
}
